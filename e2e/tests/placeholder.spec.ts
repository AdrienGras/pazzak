import { expect, test } from "@playwright/test";

// Placeholder de bootstrap P1 (n'ouvre pas de navigateur).
// Les vrais scénarios e2e arrivent en P7 (ROADMAP §P7).
test("placeholder e2e en place", () => {
	expect(1 + 1).toBe(2);
});
