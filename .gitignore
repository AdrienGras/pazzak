# Claude Code (local overrides)
.claude/settings.local.json

# Dépendances
node_modules/

# Builds / sorties
dist/
build/
.output/
.nitro/
.tanstack/
.vite/
*.tsbuildinfo

# Note : apps/web/src/routeTree.gen.ts (arbre de routes TanStack généré) est COMMITÉ
# — la CI lance `tsc` sans build préalable, donc le fichier doit exister. Régénéré au
# `vite dev`/`build`. Biome l'ignore (pattern *.gen.ts). Cf. QUIRKS + BACKLOG.

# Tests
coverage/
test-results/
playwright-report/
.playwright/

# Env & secrets (seul .env.example est committé)
.env
.env.local
.env.*.local

# Données locales (SQLite)
data/
*.db
*.sqlite
*.sqlite3

# Divers OS / éditeurs
.DS_Store
*.log
